#include <stdio.h>

int main(void)
{
	int n;
	int a[5];
	int *p;

	a[2] = 1024;
	p = &n;
	/*
	 * write your line of code here...
	 * - you are not aloowed to use a
	 * - you are not allowed to modify p
	 * only one statement
